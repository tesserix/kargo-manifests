---
# Kargo enforces: Project.metadata.name == namespace name. So the
# Project is called `kargo-mark8ly` to match the kargo-mark8ly
# namespace pre-created here.
apiVersion: v1
kind: Namespace
metadata:
  name: kargo-mark8ly
  labels:
    app.kubernetes.io/part-of: tesseract-platform
    app.kubernetes.io/component: release
    kargo.akuity.io/project: kargo-mark8ly
---
apiVersion: kargo.akuity.io/v1alpha1
kind: Project
metadata:
  name: kargo-mark8ly
  annotations:
    kargo.akuity.io/description: |
      mark8ly multi-tenant marketplace platform.
      One-env (prod) deploy: image bumps land directly into the
      `mark8ly` namespace via Argo CD Applications already running
      under the `mark8ly` AppProject.
---
apiVersion: kargo.akuity.io/v1alpha1
kind: ProjectConfig
metadata:
  name: kargo-mark8ly
  namespace: kargo-mark8ly
spec:
  promotionPolicies:
    - stage: prod
      autoPromotionEnabled: true
