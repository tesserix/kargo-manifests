---
# Bumps every istio Application's targetRevision to the freight's chart
# version. All gateway Applications share the `gateway` chart, so one
# freight aligns the whole mesh (including the two gateways that were
# bootstrapped at 1.24).
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: istio-prod
  namespace: kargo-infra
  annotations:
    kargo.akuity.io/color: '#476EAE'
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: istio
      sources:
        direct: true
  promotionTemplate:
    spec:
      steps:
        - uses: argocd-update
          config:
            apps:
              - name: istio-base
                namespace: argocd
                sources:
                  - repoURL: https://istio-release.storage.googleapis.com/charts
                    chart: base
                    desiredRevision: ${{ chartFrom("https://istio-release.storage.googleapis.com/charts", "base").Version }}
              - name: istio-cni
                namespace: argocd
                sources:
                  - repoURL: https://istio-release.storage.googleapis.com/charts
                    chart: cni
                    desiredRevision: ${{ chartFrom("https://istio-release.storage.googleapis.com/charts", "cni").Version }}
              - name: istio-istiod
                namespace: argocd
                sources:
                  - repoURL: https://istio-release.storage.googleapis.com/charts
                    chart: istiod
                    desiredRevision: ${{ chartFrom("https://istio-release.storage.googleapis.com/charts", "istiod").Version }}
              - name: istio-ztunnel
                namespace: argocd
                sources:
                  - repoURL: https://istio-release.storage.googleapis.com/charts
                    chart: ztunnel
                    desiredRevision: ${{ chartFrom("https://istio-release.storage.googleapis.com/charts", "ztunnel").Version }}
              - name: istio-egress-gateway
                namespace: argocd
                sources:
                  - repoURL: https://istio-release.storage.googleapis.com/charts
                    chart: gateway
                    desiredRevision: ${{ chartFrom("https://istio-release.storage.googleapis.com/charts", "gateway").Version }}
              - name: istio-ingress-gateway-internal
                namespace: argocd
                sources:
                  - repoURL: https://istio-release.storage.googleapis.com/charts
                    chart: gateway
                    desiredRevision: ${{ chartFrom("https://istio-release.storage.googleapis.com/charts", "gateway").Version }}
              - name: custom-ingress-gateway
                namespace: argocd
                sources:
                  - repoURL: https://istio-release.storage.googleapis.com/charts
                    chart: gateway
                    desiredRevision: ${{ chartFrom("https://istio-release.storage.googleapis.com/charts", "gateway").Version }}
              - name: homechef-ingress-gateway
                namespace: argocd
                sources:
                  - repoURL: https://istio-release.storage.googleapis.com/charts
                    chart: gateway
                    desiredRevision: ${{ chartFrom("https://istio-release.storage.googleapis.com/charts", "gateway").Version }}
